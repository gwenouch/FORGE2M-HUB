{
  "name": "forge2m-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev .",
    "check": "node --check src/app.js && node --check functions/api/_lib/session.js && node --check functions/api/_lib/access.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
